import 'package:flutter/material.dart';

class hardware_screen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Hardware")),
      body: Center(
        child: Text("Pantalla de Hardware"),
      ),
    );
  }
}