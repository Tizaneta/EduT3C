import 'package:flutter/material.dart';
class SoftwareScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context){
    return Scaffold(
      appBar: AppBar(title: Text("Software")),//aca para practicar voy a hacer lo mismo con software xddddd
      body: Center(
        child: (
          Text("Bienvenido a Software"))
      ),
      );
  }
}